all:
  children:
    wordpress:
      hosts:
        localhost:
          ansible_port: 2222
          ansible_user: sshuser
          ansible_become_password: carlos
          ansible_ssh_private_key_file: ~/.ssh/iac
